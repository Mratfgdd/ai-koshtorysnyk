; Copy to the backend app root as .user.ini on PHP-FPM / suPHP hosts,
; where php_value in .htaccess is ignored. Takes up to 5 min to apply.
upload_max_filesize = 200M
post_max_size = 200M
max_execution_time = 600
max_input_time = 600
memory_limit = 512M
