[Unit]
# Webuzo's restart routine kills every process named nginx, the system one
# included. Without this the unit stayed dead until someone noticed.
StartLimitIntervalSec=0

[Service]
Restart=always
RestartSec=2
