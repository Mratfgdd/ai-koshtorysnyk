# Base URL of the backend. Leave empty for local dev (Vite proxies /api)
# and when the backend serves the built SPA itself.
# On Vercel set it to your Render URL, without a trailing slash:
VITE_API_BASE_URL=
